<!--index.wxml-->
<van-tabs active="{{ active }}" bind:change="onChange">
  <van-tab title="{{item.className}}" wx:for="{{classList}}">
    <productlist list="{{list}}" bind:go='go' />
  </van-tab>
  
</van-tabs>
<login />
